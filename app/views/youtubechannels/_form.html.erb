<%= form_with(model: youtubechannel, local: true) do |form| %>
  <% if youtubechannel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(youtubechannel.errors.count, "error") %> prohibited this youtubechannel from being saved:</h2>

      <ul>
        <% youtubechannel.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :subscriber %>
    <%= form.number_field :subscriber %>
  </div>

  <div class="field">
    <%= form.select :youtuber_id, @youtubers_hash, {}, {required: true} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
